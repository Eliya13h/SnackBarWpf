<Window x:Class="WpfSnackBar.NotificationWindow"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="300" Height="80"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft"
        >
    <Window.Resources>
        <FontFamily x:Key="FontAwesomeFree">pack://application:,,,/Resources/Fonts/#Font Awesome 6 Free Solid</FontFamily>
    </Window.Resources>
    <Border x:Name="MainBorder" 
            Opacity="0.8"
           >
        <DockPanel>
            <TextBlock x:Name="IconImage"
                       FontFamily="{StaticResource FontAwesomeFree}"
                       FontSize="24"
                       Text="&#x0021;"
                       Height="32" 
                       Width="32"
                       TextAlignment="Center"
                       VerticalAlignment="Center"
                       Margin="20 0 " 
                       HorizontalAlignment="Right" />
            <TextBlock x:Name="MessageText"
                       TextWrapping="Wrap"
                       FontSize="14"
                       VerticalAlignment="Center"
                       FontFamily="Vazirmatn" 
                       Width="auto"/>
        </DockPanel>
    </Border>
</Window>
